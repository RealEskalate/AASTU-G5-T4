package routes

import (
	// "A2SVHUB/api/controllers"

	"github.com/gin-gonic/gin"
)

func SetupGroupGroup(api *gin.RouterGroup) {
	// group := api.Group("/group")
	// group.GET("", controllers.GetGroup)
}
